<header class="content">
    <div class="col-md-12 container clearfix" style="text-align:center">
        <h1>Exercício 2</h1><br>
    </div>
    <div>
        <h4 class="text-center">
            Crie um programa que ao receber um valor numérico de um formulário
        </h4>
        <h4 class="text-center">
            <strong>(GET)</strong> exiba a tabuada desse número na tela.
        </h4>
    </div>
</header><br><br>

<form method="post" action="/exerc2">
    <div class="row">
        <div class="col-md-6 col-md-offset-1">
            <p>Digite um número de 1 a 10 para que seja exibida a tabuada :</p>
            <input type="number" name="numero" min="0" max="10" />

            <button type="submit" class="btn btn-primary">Enviar</button>
        </div>
    </div>
    </div>
    {{#if erro}}
        <div class="col-md-5 col-md-offset-1">
            <p class="alert alert-danger" role="alert">{{mensagemErro}}</p>
        </div>
    {{else}}
        {{#each tabuada}}
            <p class="text-center"><strong>{{this}}</strong></p>
        {{/each}}
    {{/if}}
</form>